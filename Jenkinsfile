pipeline {
    agent any
    stages {
        stage('conf') {
            steps {
                sh 'echo 5ra'
            }
        }
        // stage('Run') {
        //     steps {
        //         sh 'terraform apply -auto-approve'
        //     }
        // }
        //hi tany l2aaa
    }
}
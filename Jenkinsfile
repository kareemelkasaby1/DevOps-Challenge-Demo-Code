pipeline {
    agent any
    stages {
        stage('conf') {
            steps {
                sh 'docker-compose up --build'
            }
        }
        // stage('Run') {
        //     steps {
        //         sh 'terraform apply -auto-approve'
        //     }
        // }
        //hi tany l2aaa
    }
}